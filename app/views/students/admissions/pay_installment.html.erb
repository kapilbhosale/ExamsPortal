<!DOCTYPE html>
<html lang="en">
  <head>
    <title>RCC Admissions</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

    <script type="text/javascript">
      function showHideCourses(center) {
        const courses = ['p', 'b', 'pc', 'pcb']
        if (center === 'nanded') {
          courses.forEach(function(course) {
            var element = document.getElementById(course);
            element.classList.add("d-none");
          });
        }
        else {
          courses.forEach(function(course) {
            var element = document.getElementById(course);
            element.classList.remove("d-none");
          });
        }
      }
    </script>

  </head>
  <body>

    <div class="container" style="background-color: white;">
      <% logo_props = get_logo(request.subdomain) %>
      <div class="row" style="border-bottom: 1px solid lightgray; padding-bottom: 1px;">
        <div style="padding-left: 20px;">
          <%= image_tag logo_props[:logo], width: 100, height: 50 %>
        </div>
        <div style="padding-top: 20px; padding-left: 20px;">
          <p class="pull-right" style="font-size: large;">RCC Payments page</p>
        </div>
      </div>

      <% if flash[:error] %>
      <div style="color: red;">
        <ul>
          <% flash[:error].each do |error| %>
            <li>
              <%= error %>
            </li>
          <% end %>
        </ul>
      </div>
      <% end %>

      <%= form_tag create_installment_students_admissions_path, method: :post, class: 'needs-validation form-horizontal' do %>
        <div class="form-group text-center">
          <p>Existing Students</p>
        </div>
        <div class="form-group">
          <input type="number" class="form-control" id="name" placeholder="Roll Number" name="roll_number" required>
        </div>
        <div class="form-group">
          <input type="number" class="form-control" id="parent_mobile" placeholder="Parent mobile Number" name="parent_mobile" required>
        </div>

        <div class="text-center">
          <button type="submit" class="btn btn-warning">Submit</button>
        </div>

        <div class="form-group text-center" style="color: gray; font-size: small; padding-top: 10px;">
          <p>Existing students, can Pay Installment or Purchase new course.</p>
        </div>

        <hr>
        <div style="background-color: rgb(226, 255, 231); padding: 10px;">
          <div class="form-group text-center">
            <p>New Students, please click below button.</p>
          </div>

          <div class="text-center">
            <a href="/new-admission">
              <button type="button" class="btn btn-info">New Admission</button>
            </a>
          </div>
          <div class="form-group text-center" style="color: gray; font-size: small; padding-top: 10px;">
            <p>First time students, If you do not have any admission with RCC.</p>
          </div>
        </div>
        <hr>
      <% end %>
    </div>
  </body>
</html>