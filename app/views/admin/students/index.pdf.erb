<div class="col-12">
  <div>
    <div class="card-header text-center border-0">
      <h4 class="border-0"><%=  CLASS_NAME %></h4>
      <p><%= CLASS_ADDRESS %></p>
    </div>
    <% if @students.present? %>
      <table class="table table-vcenter text-nowrap">
        <thead>
          <tr>
            <th>Roll Number</th>
            <th>Batch(s)</th>
            <th>Name</th>
            <th>Email</th>
            <th>Password</th>
            <th>Parent Mobile</th>
          </tr>
        </thead>
        <tbody>
          <% @students.each do |student| %>
            <tr>
              <td><%= student.roll_number %></td>
              <td><%= student&.batches&.each&.map(&:name).join(', ') %></td>
              <td><%= student.name %></td>
              <td><%= link_to student.email, admin_student_path(student) %></td>
              <td><%= student.raw_password %></td>
              <td><%= student.parent_mobile %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      <div class="alert alert-info">No students were found.</div>
    <% end %>
  </div>
</div>
