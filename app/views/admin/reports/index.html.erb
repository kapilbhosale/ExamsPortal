<div class="row">
  <div class="col-12">
    <div class="card">
      <div class="card-header">
        <div class="col-md-2">
          <h5 class="pt-3"> Reports</h5>
        </div>
      </div>
      <div class="card-body">
        <div class="table-responsive mt-5">
          <table class="table table-striped table-bordered table-hover table-fixed">
            <thead>
            <tr>
              <th>Name</th>
              <th>Description</th>
              <th>Question</th>
              <th>Added Ques</th>
              <th>Time</th>
              <th>Added on</th>
              <th>Reports</th>
            </tr>
            </thead>
            <tbody>
              <% if @exams.present? %>
                <% @exams.each do |exam| %>
                  <tr>
                    <td><%= link_to exam.name, admin_exam_path(exam.id) %></td>
                    <td class="text-truncate"><%= exam.description %> ...</td>
                    <td><%= exam.no_of_questions %></td>
                    <td><%= exam.questions.count %></td>
                    <td><%= exam.time_in_minutes %> mins</td>
                    <td><%= exam.created_at.to_date %></td>
                    <td class="text-center">
                      &nbsp;
                      <%= link_to ' ', admin_report_path(exam, format: 'csv'), 'data-toggle': 'tooltip', title: 'Show Report', class: 'fa fa-list' %>
                      &nbsp;&nbsp;
                    </td>
                  </tr>
                <% end %>
              <% else %>
                <tr>
                  <td colspan="8" class="text-center">No exams found, Add exams </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- you need to include the shieldui css and js assets in order for the components to work -->
<link rel="stylesheet" type="text/css" href="http://www.shieldui.com/shared/components/latest/css/light/all.min.css" />
<script type="text/javascript" src="http://www.shieldui.com/shared/components/latest/js/shieldui-all.min.js"></script>
<script type="text/javascript" src="http://www.shieldui.com/shared/components/latest/js/jszip.min.js"></script>

<script type="text/javascript">
  jQuery(function ($) {
    $("#exportButton").click(function () {
      // parse the HTML table element having an id=exportTable
      var dataSource = shield.DataSource.create({
        data: "#exportTable",
        schema: {
          type: "table",
          fields: {
            SrNo: {type: String},
            StudentName: {type: String},
            Batch: {type: String},
            Marks: {type: String},
            rank: {type: String},
            correct: {type: String},
            wrong: {type: String},
          }
        }
      });

      // when parsing is done, export the data to PDF
      dataSource.read().then(function (data) {
        var pdf = new shield.exp.PDFDocument({
          author: "PrepBootstrap",
          created: new Date()
        });

        pdf.addPage("a4", "portrait");

        pdf.table(
          5,
          5,
          data,
          [
            {field: "SrNo", title: "Sr NO.", width: 50},
            {field: "StudentName", title: "Student ", width: 200},
            {field: "Batch", title: "Batch", width: 100},
            {field: "Marks", title: "Marks ", width: 50},
            {field: "rank", title: "rank", width: 50},
            {field: "correct", title: "correct ", width: 50},
            {field: "wrong" , title: "correct ", width: 50},
          ],
          {
            margins: {
              top: 50,
              left: 2
            }
          }
        );

        pdf.saveAs({
          fileName: "StudentReport"
        });
      });
    });
  });
</script>

<style>
  #exportButton {
    border-radius: 0;
  }
</style>
