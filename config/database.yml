default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
  username: postgres
  password: password

development:
  <<: *default
  database: smart-exams-dev

test:
  <<: *default
  database: smart-exams-test
